[package]
name = "card-stake"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
testcontainers = "0.24.0"
testcontainers-modules = { version = "0.12.0", features = ["anvil"] }

[dependencies]
alloy = { version = "0.13.0", features = ["sol-types", "contract"] }
